name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Dependencies
        run: |
          npm install
          # ローカルの node_modules に CLI を確実に入れる
          npm install @capacitor/cli @capacitor/android @capacitor/core

      - name: Prepare Web Assets
        run: |
          mkdir -p www
          echo '<html><body>POZORA OS</body></html>' > www/index.html

      - name: Capacitor Update and Sync
        run: |
          # npxを使わず直接バイナリを指定
          node ./node_modules/@capacitor/cli/bin/cap add android || true
          node ./node_modules/@capacitor/cli/bin/cap sync android

      - name: Build Android APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: pozora-app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
